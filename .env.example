# Puerto en el que escucha el servidor HTTP / Socket.IO
PORT=3000

# Ruta (relativa a la raíz del proyecto) del archivo con los turnos crudos
TURNOS_FILE_PATH=./src/data/turnos.json

# Origen permitido para CORS (cliente del front-end / socket)
CORS_ORIGIN=http://localhost:5173
